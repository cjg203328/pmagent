# 概览：三项"未来方向"落地（2026-07-14）

基于 `ARCHITECTURE_OPTIMIZATION.md` §6 的"仍可继续方向"，本次把三项全部实现并接入。
全部**纯增量、可插拔、默认不改变既有行为**（开关关闭即回到原行为）。

## 交付内容

### 1. LLM 任务分类器 → 分层模型
- `artpm_agent/routing/task_classifier.py`（新增）：`LLMTaskClassifier`，输出 chat/reasoning/vision/routing，LRU 缓存，关闭返 "chat"，非法标签回退启发式。
- `artpm_agent/providers/gateway.py`：`classify_task()` 接入 `chat/stream_with_failover`；`task_type is None 且分类器已启用` 时自动分类 → `best_model_for_task` 选分层模型。
- 启用：`ARTPM_TASK_CLASSIFIER=1|true|yes|on` 或 `llm_config["task_classifier_enabled"]=True`。
- 防回归：关闭时 `task_type` 保持 `None`，**不触发** `best_model_for_task` 重排序，保留原 failover 顺序。

### 2. 遥测运营看板
- `artpm_agent/runtime/telemetry.py`：`by_task_type / by_model / latency_trend`（升序）。
- `artpm_agent/runtime/telemetry_dashboard.py`（新增）：`collect_dashboard` + `render_html`（自包含浅色 HTML + SVG sparkline）+ CLI。
- `telemetry_dashboard_app.py`（新增）：独立 Streamlit 入口，只读增量，不动 `app.py`。
- 用法：`python -m artpm_agent.runtime.telemetry_dashboard --db data/telemetry.db --out report.html --window 500`

### 3. MemoryContextBudget token 压缩扩展（Pi Phase 4）
- `artpm_agent/harness/token_budget.py`（新增）：`estimate_tokens`（tiktoken→CJK 启发式）、`compact_text`（去重/折叠，安全边界以原始长度为基准）、`apply_token_budget`（先丢最低优先级整块→compact/summarize/二分 token 截断）。
- `artpm_agent/harness/memory_retrieval.py`：`MemoryContextBudget` 加 `max_total_tokens/compression/token_model/token_counter/summarize_fn`；`inject_memory_context` 加 `max_context_tokens`（>0 才走 token 路径）。
- `artpm_agent/internal/chat_harness_integration.py`：透传 `memory_inject_max_tokens`。
- 默认 `0`（关闭）行为不变。

### 4. 顺手修掉的预存 bug
- `artpm_agent/artifacts/coordinator.py`：LLM 调用抛 `RuntimeError`（离线）时未被 `except` 捕获 → "离线从模板生成"路径中断；已在 `except` 补 `RuntimeError`（与 `..._from_it_offline` 测试意图一致）。

## 测试与回归
- 三特性测试：test_task_classifier(12) + test_telemetry_dashboard(7) + test_token_budget(13) = **32 全绿**。
- 修复过程中暴露/解决的回归：7 个 gateway/regression 用例（分类器关闭时误触发重排序）+ 1 个 coordinator 离线用例。
- **全量回归（清 .pyc 复跑）：696 passed / 2 skipped**（2 skip = `ART_ENABLE_INTEGRATION=1` 门控集成测试）。

## 文档
- `ARCHITECTURE_OPTIMIZATION.md` §6 改写为"未来方向落地状态"，标记三项完成 + 启用方式 + 回归说明。

## 提交纪律
工作树含用户未提交 WIP（agent.py / config.py / views 等）；本次**未提交任何文件**，改动纯增量、默认关闭。
