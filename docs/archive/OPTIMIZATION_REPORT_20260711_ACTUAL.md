# ArtPM Agent 实际审计与优化报告

日期：2026-07-11

## 项目理解

ArtPM Agent 是面向游戏美术外包项目管理的 Streamlit 应用。主要调用链为：

1. `app.py` 提供对话、概览、项目、Excel 上传和设置页面。
2. `agent.py` 负责意图识别，并通过 `SkillRouter` 调用利润、任务、进度、提醒和文档技能。
3. SQLAlchemy 业务库保存 UI 项目数据；轻量 SQLite 记忆库保存文档、人员和检索数据。
4. 本地 MCP 工具提供文件读取、搜索、数据分析和受控命令能力。

## 已落地的优化

- 修复同步路由调用异步 Skill 时返回协程、执行失败的问题。
- 保留 Skill 明确返回的失败状态，不再强制覆盖为成功。
- 修复 Streamlit `UploadedFile` 无法被 Excel 解析器处理的问题。
- 修复“项目名称”元数据行被误判为资产表头的问题，并兼容空行、`.xls` 和结构化返回值。
- 将业务数据库与记忆数据库拆分为 `artpm.db` 和 `memory.db`。
- 取消 schema 不匹配时自动删表，改为显式迁移错误，避免数据丢失。
- 为轻量 SQLite 动态表名和字段名加入白名单，并启用外键、超时和查询索引。
- 将轮询任务分配替换为技能、负载、经验综合评分，真正执行最大负载限制。
- 将进度 Skill 接入真实业务数据库，修复 ORM 项目缺少 `progress/get()` 时崩溃的问题。
- 移除伪造的远程 MCP 成功结果；Agent 现在使用真实本地工具，远程服务只在端点、Key 和开关均配置时启用。
- 限制本地工具访问工作区以外路径，默认禁用命令执行，并移除 `shell=True`。
- 移除验证器中的 `eval`，改为受限比较规则解析。
- 占位 API Key 不再被当作有效配置；确定性关键词路由优先于付费 LLM 路由。
- 设置页改为更新根目录 `.env`，且保留未管理的配置项和注释。
- Windows GBK 终端运行健康检查不再因 emoji 抛出 `UnicodeEncodeError`。
- 核心依赖与 OCR/FAISS 等可选重依赖分离，启动脚本改用统一依赖清单。
- 补全全部 10 个 Skill 的聊天意图路由；文件搜索、读取、数据、趋势和项目评估可从对话触发。
- 趋势 Skill 改为真实斜率、变化率和数值预测；项目评估支持结构化文件、数字与工期校验。
- 文档 Skill 不再返回固定报价单，改为真实 Excel/TXT/CSV/JSON 解析或明确拒绝不支持格式。
- 修复缓存 `None`、异步函数、TTL、并发写入问题，并增加离线文档检索回退。
- 修复任务真实负载、无排期项目分类、企业微信真实投递状态和 Token 周/月预算统计。
- 修复 ORM 脱离 Session 后任务负责人无法序列化的问题。
- 修复甘特图日期轴、零容量热力图和显式报价合计优先级。
- 设置页现在读取当前提供商、模型、费率和端点，支持智谱且不会用空输入覆盖已有 Key。
- 默认 Web 服务只监听 `127.0.0.1`，避免无认证配置界面暴露到局域网。
- `restart.bat` 不再删除业务数据库，只停止服务并保留全部数据和配置。
- UI 与 Agent 复用同一 `DB_PATH` 业务库，健康检查也验证实际配置路径。
- 任务分配不再为空数据库伪造演示成员，并实现真实历史协作加分。

## 验证结果

- `python -m pytest -q`：48 passed。
- `python -m compileall -q artpm_agent tests`：通过。
- `ruff check artpm_agent tests`：通过。
- Streamlit `AppTest`：首屏无异常，正确进入“智能助手”。
- Streamlit 五个视图及离线利润聊天交互：无异常。
- 健康检查：全部核心组件正常；第三方 OpenAI 兼容接口已使用 `qwen3.5` 完成真实对话验证。
- Web 和 CLI 两个公开入口均完成冒烟验证。

## 后续策略

1. 数据层：为业务库引入 Alembic；任何 schema 变更都通过版本化迁移执行。
2. 测试层：为 Streamlit 上传和设置保存增加交互测试，将覆盖率目标提升到核心模块 70% 以上。
3. 能力层：OCR、FAISS、远程 MCP 作为可选 extra 安装，不影响核心离线功能。
4. 产品层：上传报价单后增加人工确认和“保存为项目”流程，避免解析结果直接污染业务数据。
5. 运维层：增加 `.env.example`、结构化日志脱敏、CI 测试和数据库备份说明。

## 已知边界

- 当前 LLM 配置为 `custom`，Base URL 是 `https://api.zhongteai.cn/v1`，模型为 `qwen3.5`；Key 仅保存在本机 `.env`。
- 未配置有效 LLM Key 时，通用自由对话不可用，但利润、任务、进度等确定性 Skill仍可离线使用。
- OCR 与 FAISS 未安装时自动降级，不属于核心启动故障。
- 远程 Skills Forge 需要真实的 `SKILLS_FORGE_URL` 和 API Key；项目不再返回模拟结果。
- `app_old.py` 等旧页面及早期 `core/chat_agent.py` 实验架构不属于当前受支持入口，已在源码 README 和 Ruff 配置中明确隔离。
