# 2026-07-12

## 项目诊断与优化策略
- 对 ArtPM Agent（游戏美术外包 PM Streamlit 助手）做了完整静态扫描：71 个 .py / 25,829 行；`app.py` 3528 行（42 处宽异常）。
- 关键发现（与之前 OPTIMIZATION_REPORT 矛盾）：
  - `tests/` 为空，pytest.ini 的 `testpaths = artpm_agent tests` 导致 CI 实际收集 0 测试；7 个 test_*.py 散落根目录且都是 MCP 连通性测试，无业务逻辑单测。
  - 死代码约 2600 行：app_old/backup/simple + 5 个"实验" core 文件 + mcp_client.py / mcp_client_real.py（生产只用 mcp_client_enhanced）。
  - 无 pyproject/setup，14 处 sys.path.insert；非 git 仓库但已有 .github/workflows/ci.yml（从未触发）。
  - DB 无 Alembic，SQLAlchemy 1.x，schema 变更即丢数据风险；两套 LLM client 重复。
- 产出：`项目诊断与优化策略.md`（健康度评分 + 5 阶段路线图 P0/P1/P2 + 5 个快赢）。
- 待用户确认推进方式（A 快赢 / B 阶段0+1 / C Plan 详细计划）。
