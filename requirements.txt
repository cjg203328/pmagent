# Runtime dependencies are managed by pyproject.toml as the single source.
# Recommended installation:
#   pip install -e .                # Core Runtime/CLI only
#   pip install -e ".[ui,documents,vector-local]" # Offline Streamlit UI
#   pip install -e ".[ui,api,documents,vector-local,llm-openai]" # Local stack
#   pip install -e ".[production]" # Production composite
#   pip install -e ".[ocr,vector-local]" # Optional OCR and vector extras.
#   pip install -e ".[mineru]"      # Optional pinned MinerU pipeline runtime.
#   pip install -e ".[mineru-client]" # Lightweight remote MinerU client.
#   pip install -e ".[production,dev]" # Full test environment.
-e .
