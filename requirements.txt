# 依赖现由 pyproject.toml 的 [project].dependencies 统一管理（单一来源）。
# 推荐安装方式：
#   pip install -e .                 # 仅核心依赖
#   pip install -e ".[ocr,vector]"   # 额外安装可选 OCR / 向量检索
#   pip install -e ".[dev]"          # 额外安装开发/测试依赖
-e .
