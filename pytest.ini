[pytest]
asyncio_mode = auto
testpaths = artpm_agent tests
markers =
    integration: 需要本地 MCP server / 网络 / API 密钥的集成测试，默认跳过（设 ART_ENABLE_INTEGRATION=1 运行）
addopts = -ra --cov=. --cov-report=term-missing --cov-report=xml --cov-fail-under=50
